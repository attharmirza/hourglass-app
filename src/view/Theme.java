package view;

import java.awt.*;

/**
 * Class containing theme colors for the application. Centralized so that a new
 * theme can be applied easily.
 */
public class Theme {
    public static final Color BACKGROUND_COLOR = new Color(21, 78, 148);
    public static final Color FOREGROUND_COLOR = new Color(201, 44, 89);
}
